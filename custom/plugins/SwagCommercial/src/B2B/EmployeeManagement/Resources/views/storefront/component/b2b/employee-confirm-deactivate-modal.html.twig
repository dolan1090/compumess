{% block b2b_components_employee_deactivate_modal %}
    {% set options = {
        contentId: 'b2b-employee-deactivate-modal-content',
        confirmButtonId: 'b2b-employee-deactivate-modal-confirm-button',
    } %}

    <div
        id="b2b-employee-deactivate-modal"
        class="modal fade"
        tabindex="-1"
        aria-labelledby="b2b-employee-deactivate-modal-title"
        aria-hidden="true"
        data-b2b-employee-deactivator
        data-b2b-employee-deactivator-options="{{ options|json_encode() }}"
    >
        <div class="modal-dialog">
            <div class="modal-content">
                {% block b2b_components_employee_deactivate_modal_header %}
                    <div class="b2b-employee-deactivate-modal-header modal-header">
                        <h5 class="modal-title" id="b2b-employee-deactivate-modal-title">
                            {{ "employee-management.employeeDeactivate.title"|trans|sw_sanitize }}
                        </h5>
                        <button
                            type="button"
                            class="btn-close"
                            data-bs-dismiss="modal"
                            aria-label="Close"
                        ></button>
                    </div>
                {% endblock %}

                {% block b2b_components_employee_deactivate_modal_body %}
                    <div class="b2b-employee-deactivate-modal-body modal-body">
                        <div id="{{ options.contentId }}" class="b2b-employee-deactivate-modal-content text-center">
                            <div id="b2b-employee-deactivate-modal-loading" class="spinner-border" role="status"></div>
                        </div>
                    </div>
                {% endblock %}

                {% block b2b_components_employee_deactivate_modal_footer %}
                    <div class="b2b-employee-deactivate-modal-footer modal-footer">
                        <button
                            type="button"
                            id="b2b-employee-deactivate-modal-cancel-button"
                            class="btn btn-light"
                            data-bs-dismiss="modal"
                        >
                            {{ 'global.default.cancel'|trans|sw_sanitize }}
                        </button>
                        <button
                            type="button"
                            id="{{ options.confirmButtonId }}"
                            class="btn btn-primary"
                        >
                            {{ 'global.default.deactivate'|trans|sw_sanitize }}
                        </button>
                    </div>
                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}
