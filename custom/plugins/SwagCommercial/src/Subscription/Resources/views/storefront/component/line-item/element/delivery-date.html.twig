{% sw_extends {
    template: "@Storefront/storefront/component/line-item/element/delivery-date.html.twig",
    scopes: ['default', 'subscription']}
%}

{% block component_line_item_delivery_date %}

    {% set subscription = context.extensions.subscription ? context.extensions.subscription : page.order.extensions.subscription %}

    {% if not (subscription and getLicense('SUBSCRIPTIONS-6549379')) %}
        {{ parent() }}
    {% endif %}

{% endblock %}
