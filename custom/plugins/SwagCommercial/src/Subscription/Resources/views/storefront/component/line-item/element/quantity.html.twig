{% sw_extends {
    template: "@Storefront/storefront/component/line-item/element/quantity.html.twig",
    scopes: ['subscription']
} %}
{% block component_line_item_quantity %}
    {% if getLicense('SUBSCRIPTIONS-6549379') %}
        {% set formAction = path('frontend.subscription.checkout.line-item.change-quantity', { 'id': lineItem.id, subscriptionToken: context.token }) %}
    {% endif %}

    {{ parent() }}
{% endblock %}
