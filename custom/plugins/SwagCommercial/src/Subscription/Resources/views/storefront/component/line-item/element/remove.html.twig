{% sw_extends {
    template: "@Storefront/storefront/component/line-item/element/remove.html.twig",
    scopes: ['subscription']
} %}

{% block component_line_item_remove %}
    {% if getLicense('SUBSCRIPTIONS-6549379') %}
        {% set formAction = path('frontend.subscription.checkout.line-item.delete', { 'id': lineItem.id, subscriptionToken: context.token }) %}
    {% endif %}

    {{ parent() }}
{% endblock %}
